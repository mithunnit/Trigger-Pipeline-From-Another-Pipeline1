trigger:
- main

pool:
#  vmImage: ubuntu-latest
  name: azuredevops-scaleset

jobs:
  - job: job1
    steps:
      - script: echo Hello, $HOSTNAME
  
  - job: job2
    steps:
      - script: echo Hello, $HOSTNAME
    
  - job: job3
    steps:
      - script: echo Hello, $HOSTNAME
  
  - job: job4
    steps:
      - script: echo Hello, $HOSTNAME

  - job: job5
    steps:
      - script: echo Hello, $HOSTNAME 


